# 인수인계 — SK 그룹사 버전 (새 Claude Code 세션용)

> **이 문서를 먼저 끝까지 읽고 시작할 것.** 여기 적힌 함정은 전부 **실제로 겪어서 시간·토큰을 날린 것**이다.
> 작성 2026-08-12. 작성 시점 커밋: `cfd12dc` 이후.

---

## 1. 무엇을 만드는가

지금 이 저장소는 **SK E&S 버전** 대시보드다(회사 조별과제로 제출, `7.yes-i-can.kr`).
새로 만들 것은 **SK 그룹사 버전** — 상단에서 계열사를 고르면 그 회사 기준으로 보여주는 것.

```
[ SK E&S ] [ SK하이닉스 ] [ SK텔레콤 ] ...   ← 계열사 선택
     ↓
  그 회사에 맞는 사업 카테고리 + 키워드로 수집·분류된 결과
```

### 🔑 핵심 설계 결정 (이게 제일 중요하다)

**회사마다 다른 건 "키워드 세트 + 사업 카테고리 세트"뿐이다. 나머지 로직은 전부 똑같다.**

| 구분 | 회사마다 다른가 |
|---|---|
| 검색 키워드 (수집 사전필터) | ✅ **다름** |
| 사업 카테고리 (SK E&S = 전력·LNG·재생E·수소·도시가스·원전·에너지솔루션·분산에너지 8개) | ✅ **다름** |
| 회의록 수집·파싱 | ❌ 같음 (원문 코퍼스 자체가 공유) |
| AI 판정 로직·성향(우호/중립/비우호) | ❌ 같음 |
| 기사 검색·라벨링 | ❌ 같음 (질의 키워드만 다름) |
| 인물 랭킹·보드(사업×성향) | ❌ 같음 |
| 화면·모달·지도 | ❌ 같음 |

→ **코드를 계열사별로 복제하지 말 것.** 회사를 **설정(config)** 으로 빼고 파이프라인은 하나로 유지한다.
→ **SK E&S 는 계열사 중 하나로 들어간다.** 지금 데이터가 그대로 "SK E&S 탭"이 되므로 **재수집·재판정 불필요.**

---

## 2. 지금 상태 (승계되는 것)

### (a) 저장소 안 — clone 하면 따라온다

| 자산 | 규모 | 승계 |
|---|---|---|
| `data/assembly_speeches.json` **에너지로 걸러진** 국회 발언 | 12,891건 (SK E&S 용) | ✅ 그대로 |
| `data/_assembly_tagged.json` AI 판정 | 12,891건(채택 5,127) · **내용 기반 key 보유** | ✅ 그대로 |
| `data/_cab_results.json` 청와대 판정 + `judgedMeetings` 대장 | 발언 194건 · 판정완료 78회의 | ✅ 그대로 |
| `data/news.json` 기사 | 420건 · NAS가 하루 5회 자동수집 | ✅ 그대로 |
| `web/data.js` 국회 300명(발언 4,712건·AI분석 224명) | — | ✅ SK E&S 탭이 됨 |

### (b) 🔴 저장소 **밖** — `C:\AI\_corpus\` (워크스페이스 공용, git 밖)

**여기가 그룹사 버전의 진짜 토대다.** clone 으로 안 따라오니 새 방에서도 **같은 경로를 그대로 보면 된다**(재수집 불필요).

| 폴더 | 내용 | 규모(2026-08-12 수집 시점) |
|---|---|---|
| `_corpus/assembly_raw/` | 국회 상임위 발언 **무필터 원본** xlsx | **1,118,676건** · 1,000여 파일 · 수백 MB |
| `_corpus/cabinet_raw/` | 국무·차관회의 회의록 원본 | **466개** (PDF 102 + HWPX 364) · 2022-12 ~ 2026-06 |

- 규칙·경로는 상위 `C:\AI\CLAUDE.md` 의 "📦 여러 방이 같이 쓰는 원문" 절과 `_corpus/README.md` 참고.
- 경로는 `--raw-dir=`/`--dir=`(env `SPEECH_RAW_DIR`/`CABINET_RAW_DIR`)로 바꿀 수 있다. 하드코딩 금지.

⚠️ 청와대 회의록은 **2023년치까지 전부 다시 받을 수 있다**(2026-08-12 확인). 한때 "옛것은 못 받는다"고
적었으나 오판이었고, 실제 원인은 WAF 레이트리밋이었다 — `update-cabinet.mjs` 에 간격·재시도가 들어가 있다.

---

## 3. 1차 작업 — SK E&S 하드코딩을 설정으로 빼기

지금 코드는 SK E&S 전용이 여기저기 박혀 있다. **실측 위치:**

**(a) 사업 카테고리(8개 BIZ) 가 박힌 곳** — 계열사별로 갈라야 함
```
pipeline/lib/config.mjs            META.businesses
pipeline/lib/news_labels.mjs       NEWS_LABELS (라벨·색·키워드), QUERY_TERMS
pipeline/build_ai_input.mjs        BIZLABEL
pipeline/build_assembly_speeches.mjs / build_board.mjs / build_board_input2.mjs / build_cabinet2.mjs   BIZ 배열
pipeline/wf_cabinet.js             SCHEMA enum + 프롬프트의 8개 사업 정의문
web/data.js  web/cabinet.js  web/bizboard.js  web/news.js   (생성물 — 재생성되므로 코드만 고치면 됨)
```

**(b) 검색 키워드가 박힌 곳**
```
pipeline/lib/config.mjs            ENERGY_KEYWORDS  ← 수집 사전필터(서버검색 + 로컬 정규식)
pipeline/lib/news_labels.mjs       QUERY_TERMS      ← 기사 검색 질의
pipeline/collect_assembly_speeches.mjs / 5_collect_news.mjs / 6_fetch_excerpts.mjs  (위 둘을 import)
```

**(c) "SK E&S" 문구가 박힌 곳** — 프롬프트·화면 문구
```
pipeline/wf_ai.js  wf_board.js  wf_cabinet.js      "너는 SK E&S(...)의 정책 애널리스트다"
pipeline/build_assembly_speeches.mjs               meta.analysisNote
```

### 권장 방향
`pipeline/lib/companies/<회사>.mjs` 같은 파일 하나에 회사별로 `{ id, label, businesses[], keywords[], newsLabels[], promptIntro }`
를 모으고, 위 스크립트들이 `--company=skens` 인자로 그걸 골라 쓰게 한다.
산출물도 회사별로 분리(`web/data_skens.js` · `web/data_skhynix.js` …), 화면은 탭으로 전환.

⚠️ **회의록 원문(`assembly_speeches.json`)은 회사별로 나누지 말 것.** 원문은 공유하고 *판정 결과만* 회사별로 갖는다.
안 그러면 같은 회의록을 계열사 수만큼 중복 수집·저장하게 된다.

---

## 3.5 대상 계열사 (2026-08-12 확정) + 카테고리 초안

**9개로 간다: SK E&S · SK이노베이션 · SK텔레콤 · SK하이닉스 · SK에코플랜트 · SK가스 · SK바이오팜 · SK네트웍스 · SK스퀘어**

사업 카테고리는 **SK E&S 만 확정**이고 나머지 8개는 아래가 초안이다 — 새 세션에서 사용자와 확정할 것.
(카테고리 개수는 회사마다 달라도 된다. 화면은 개수에 안 묶여 있다.)

| 계열사 | 사업 카테고리 | 주력 상임위 | 상태 |
|---|---|---|---|
| **SK E&S** | 전력 · LNG · 재생E · 수소 · 도시가스 · 원전 · 에너지솔루션 · 분산에너지 (8) | 기후위·산자위 | ✅ **확정·데이터 완비** |
| SK이노베이션 | 정유·석유화학 / 배터리(SK온) / 분리막·소재 / 윤활유 / 자원개발 / 폐배터리·리사이클 / 탄소중립·CCUS | 산자위·기후위 | 🟡 초안 |
| SK텔레콤 | 통신(주파수·요금제·망) / AI·데이터센터 / 미디어·콘텐츠 / 보안 / 클라우드 / UAM | **과방위** | 🟡 초안 |
| SK하이닉스 | 메모리(DRAM·NAND) / HBM·AI반도체 / 소부장 / 팹투자·용인클러스터 / 수출규제·통상 / 팹 전력·용수 | 산자위·과방위 | 🟡 초안 |
| SK에코플랜트 | 환경(폐기물·수처리) / 건설·플랜트 / 재생에너지 EPC / 반도체 인프라 / 데이터센터 | 국토위·기후위 | 🟡 초안 |
| SK가스 | LPG / LNG·수소 / 발전(청라·울산) / 에너지 인프라 | 산자위·기후위 | 🟡 초안 |
| SK바이오팜 | 신약·임상 / 약가·급여 / 제약 규제(식약처) / 바이오 R&D 지원 | **보건복지위** | 🟡 초안 |
| SK네트웍스 | AI·신사업 / 렌탈 / 유통·모빌리티 / 워커힐(관광) | 산자위·정무위 | 🟡 초안 |
| SK스퀘어 | 반도체·ICT 투자 / 지주 규제 / 자회사(하이닉스·11번가 등) | 정무위·과방위 | 🟡 초안 |

---

## 3.6 원문 더미(코퍼스) — 왜 다시 모아야 했나 · ✅ 2026-08-12 수집 완료

> "코퍼스" = 분석 대상 원문 뭉치. 여기선 국회 회의록에서 뽑아 모아둔 **발언 원문 더미**.

**저장소의 `data/assembly_speeches.json` 12,891건은 "에너지 키워드로 이미 걸러진 결과"다.**
그물에 안 걸린 발언은 **저장 자체가 안 됐다.** 없는 걸 다시 분류할 수는 없다.

**실측 (2026-08-12, 기노위+산업위):**
```
서버 보유 발언 메타      92,990건
  ↓ 파싱                 91,372건
  ↓ 의원 발언만          55,159건
  ↓ ENERGY_KEYWORDS 통과  5,474건   ← 여기서 90% 가 버려진다
  최종 저장               5,067건   (원본의 5.4%)
```

**계열사별 사용 가능 여부:**

| 계열사 | 지금 원문 더미로 되나 |
|---|---|
| SK E&S · SK가스 · SK이노베이션 | 🟡 부분적 (에너지 그물에 걸린 것만) |
| **SK하이닉스 · SK텔레콤 · SK바이오팜 · SK네트웍스 · SK스퀘어** | ❌ **거의 안 됨** — "반도체 수출규제"에 에너지 단어가 없으면 애초에 미수집 |

### 확정 전략 — **원본 확보 → DB화 → 활용** (사용자 지시, 2026-08-12)

> "원본파일을 최대한 확보하고 > DB화 하고 > 그걸 기반으로 이리저리 굴려봐야지.
> 안그럼 계속 할때마다 파일 열어서 읽고 해야되잖아."

**1) 원본 확보 — ✅ 2026-08-12 완료(또는 진행 중)**
```bash
node pipeline/collect_assembly_speeches.mjs --raw-only --committees=<위원회명>   # 국회 발언
node pipeline/update-cabinet.mjs --pages=45                                      # 국무·차관회의
```
- `--raw-only` 는 `--by-keyword` **없이** 전량을 받아 원본 폴더에만 쌓는다.
  **에너지 필터를 안 걸고 `assembly_speeches.json`(SK E&S 산출물)도 안 건드린다** → 제출본 안전.
- 둘 다 **무인증·무료 · AI 안 씀 → 토큰 0.** 한 번 받아두면 계열사를 늘려도 서버에 다시 안 묻는다.
- 실측 규모: 국회 발언 **111만 건**(1,118,676), 회의록 **466개 파일**.

**2) DB화 — 다음 할 일**
🔴 **전량을 JSON 한 덩이로 만들지 말 것.** 발언 111만 건 × 평균 900자 ≈ **1GB** — 열지도 못한다.
→ **SQLite + FTS5(전문검색).** `node:sqlite` 가 **Node 22.5+ 내장**이라 **npm 설치도, 네이티브 빌드도 필요 없다**
(2026-08-12 실측 확인: `new DatabaseSync()` + `CREATE VIRTUAL TABLE ... USING fts5` 정상 동작).
NAS 컨테이너에서도 그대로 돌아간다.

권장 스키마 (원본이 원본이고 DB 는 언제든 다시 만들 수 있는 **파생물**):
```
C:\AI\_corpus\corpus.db        ← gitignore, 재생성 가능
  speeches(id, conferNum, seq, committee, date, name, memberId, text)   + FTS5(text)
  minutes (id, meeting, date, speaker, role, text, srcFile)             + FTS5(text)
```
JSON 은 **결과물에만** 쓴다 — 회사별 후보/판정처럼 커밋할 만한 크기일 때.

**3) 활용**
회사별 키워드로 DB 를 쿼리해 후보를 뽑고 **그 부분집합만** AI 판정한다.
- `data/candidates_<회사>.json` = 그 회사 판정 대상(커밋 가능한 크기)
- 계열사 추가가 **서버 요청 0 · 로컬 SQL 만으로** 끝난다. 이게 이 구조의 핵심 이득.

⚠️ `_corpus/` 는 git 밖이라 **이 PC 에만 있다.** 9개 계열사의 토대이니 **백업 경로를 정할 것**(NAS 볼륨 등).

---

### ⚠️ 이 목록에서 나오는 설계 이슈 3가지 (미리 정하고 시작할 것)

1. **한 발언이 여러 계열사에 동시에 해당된다.**
   예: "용인 반도체 클러스터 전력공급" → SK하이닉스(팹 전력) **그리고** SK E&S(전력) 둘 다.
   → 판정은 **전역 1개가 아니라 (회사 × 발언) 단위**여야 한다. 회사별로 독립된 판정 파일을 갖고,
   `tagkey` 에 회사 id 를 섞어(`skhynix|<tagkey>`) 재사용 여부를 판단할 것.
   **한 발언에 카테고리 하나만 붙이는 현재 구조를 그대로 계열사에 확장하면 이 케이스를 놓친다.**

2. **SK E&S 와 SK이노베이션은 실제로 합병된 관계다**(2024년). 그래도 사업 영역이 뚜렷이 달라
   탭을 나누는 건 문제없지만, **카테고리가 겹칠 수 있다**(예: 탄소중립·수소). 겹칠 때 어느 탭에 넣을지,
   아니면 양쪽에 다 보이게 할지 **먼저 정할 것.**

3. **비에너지 계열사는 소관 위원회가 다르다.** 위 표의 "주력 상임위" 참고.
   17개 위원회를 전량 받아뒀으니(§3.6), **판정은 그 회사 주력 위원회부터** 돌리면 적은 비용으로 화면이 먼저 찬다.
   (텔레콤=과방위, 바이오팜=보건복지위, 스퀘어=정무위처럼 에너지와 전혀 다른 곳이 주력이다.)

---

## 4. 💸 토큰 — 새 계열사 추가할 때 반드시 볼 것

새 계열사를 넣으면 **그 회사 카테고리로 판정**해야 한다. 원본이 111만 건이라 그대로 다 돌리면 매우 비싸다.

**절감 순서:**
1. **먼저 키워드로 후보를 좁힌다.** SK E&S 가 `ENERGY_KEYWORDS` 로 12,891건까지 줄인 것처럼,
   하이닉스면 반도체·소부장·팹·수출규제 …, 텔레콤이면 통신·주파수·요금제·AI인프라 … 로 사전필터.
   **AI 판정은 그 부분집합에만 돌린다.** 이게 제일 큰 절감이다.
2. **이미 판정한 건 절대 다시 보내지 않는다.** 아래 3개가 그 장치다(2026-08-12 신설):
   - `pipeline/lib/tagkey.mjs` — 위치와 무관한 **내용 기반 키**. 순번(id)으로 가리키지 말 것.
   - `build_tag_batches.mjs --only-new` — 판정 아카이브를 보고 **이미 판정한 발언을 건너뛴다.**
     (예전엔 채택분만 제외해서 **기각 7,764건이 증분마다 재판정**됐다.)
   - `pipeline/cab_todo.mjs` — 청와대에서 **아직 판정 안 한 회의만** 추린다.
     🔴 "관련 발언 없음"으로 **0건 판정된 회의는 결과가 비어서 *안 한 것*과 구분이 안 된다.**
     그래서 `_cab_results.json` 에 `judgedMeetings` 대장을 따로 둔다(46건 중 22건이 이 경우였다).
   → **회사별 판정을 새로 만들 때도 이 구조를 그대로 따라갈 것.** 회사 id 를 키에 섞으면 된다.
3. **프롬프트 캐싱** — `wf_*.js` 프롬프트는 **변하는 부분(파일 경로·인덱스)이 맨 뒤**에 있다.
   앞으로 옮기면 뒤 지시문 전체가 매번 새 토큰이 된다. 공통 접두사 실측 1,133자(wf_cabinet).
   **지시문 수정할 때 이 순서를 깨지 말 것**(각 파일에 주석 있음).

---

## 5. 🪤 반드시 알아야 할 함정 (전부 실제로 당한 것)

1. **`web/*.js` 생성물의 `window.XXX = ` 대입문을 날리지 말 것.**
   2026-08-10에 헤더 주석을 고치다 `window.APP_DATA = ` 를 통째로 날려서 **대시보드 전체가 안 떴다.**
   파일은 200 OK 로 2.8MB 가 멀쩡히 내려가고 JSON 도 유효해서 **서버·네트워크만 보면 원인이 안 보인다.**
   → 생성 스크립트의 head/tail 을 건드렸으면 **반드시 브라우저에서 실제 렌더링까지 확인**할 것. JSON 파싱 검증만으론 못 잡는다.
2. **`import` 는 곧 실행이다.** `node -e "import('./pipeline/scheduler.mjs')"` 로 로드 테스트했다가
   **스케줄러가 실제 기동해 뉴스 수집이 시작**됐다. 로드 검증은 `node --check` 까지만.
3. **AI 입력을 만들기 *전에* 뉴스 라벨링을 끝낼 것.** 순서가 뒤집혀서 3명의 AI 분석이 옛 라벨 기준으로 생성됐다.
   올바른 순서: 기사 수집 → 라벨링 → `build_ai_input` → AI 분석.
4. **`build_ai.mjs` / `build_cabinet2.mjs` 는 실패하면 기존 분석을 지운다.** 결과가 비면 그대로 덮어쓴다.
   대량 실패 가드의 본보기는 `5_collect_news.mjs` 의 "🛑 대량 실패 안전장치"(성공률 60% 미만이면 아무것도 안 씀).
5. **Workflow 도구의 `args` 가 JSON 문자열로 온다.** 객체를 넘겨도 스크립트 안에선 문자열이다.
   `const A = typeof args === 'string' ? JSON.parse(args) : args` 없으면 **조용히 빈 결과**로 끝난다.
6. **워크플로가 판정 그룹을 헷갈린다.** 보드 24그룹 중 2개가 엉뚱한 그룹 데이터를 처리했다.
   → 결과의 `biz/stance` 를 **입력 파일의 그룹 정의와 대조 검증**할 것. 인덱스만 믿지 말 것.
7. 회의록 파싱 함정(2단 조판·불릿 U+110B·필리버스터 컬럼 밀림·한 자리 월/일)은 코드에 방어가 들어가 있다.
   **지우지 말 것.** 상세: `node pipeline/findings.mjs 발언`, `AUTOMATION_TODO.md` §A.

---

## 6. 데이터 출처 (전부 무료·무인증)

| 데이터 | 출처 | 스크립트 |
|---|---|---|
| 국회 상임위 발언 | 국회도서관 발언 빅데이터 `dataset.nanet.go.kr` | `collect_assembly_speeches.mjs` |
| 국무회의·차관회의 PDF | 행정안전부 게시판 | `update-cabinet.mjs` |
| 의원 명단 | 열린국회정보 API (**키 필요**) | `1_collect_members.mjs` |
| 기사 | Google 뉴스 RSS(기본) / 네이버(키 있으면) | `5_collect_news.mjs` |

상세 URL·파라미터·함정은 `pipeline/README.md` 상단 "📡 데이터 출처" 표와 `data/findings.json`.

---

## 7. 배포 (분기 시 바꿔야 할 것)

현재 SK E&S 버전: NAS `/volume2/docker/policy` · 포트 **8007** · `7.yes-i-can.kr`
컨테이너 3개: `policy-web`(Node, 프론트+API) · `policy-scheduler`(기사 자동수집) · `policy-tunnel`(cloudflared)

**새 버전에서 반드시 바꿀 것** (안 바꾸면 NAS에서 충돌해 컨테이너가 안 뜬다):

| 항목 | 현재 | 새 버전 |
|---|---|---|
| `docker-compose.yml` `container_name` ×3 | `policy-*` | 새 이름 |
| `ports` | `8007:8137` | **8009** (2026-08-12 기준 빈 포트) |
| NAS 폴더 | `/volume2/docker/policy` | 새 폴더 |
| 서브도메인 | `7.yes-i-can.kr` | 새 것 (사용 중: `7` `family` `grid` `gukak` `invest` `omadang` `portfolio` `tax`) |
| Cloudflare 터널 | 기존 | **새 터널 생성 → 새 토큰** |

`.env`(터널 토큰)는 **NAS 안에서 `echo` 로** 만든다(Synology엔 nano 없음, 작은따옴표 필수).
상세 절차는 상위 `C:\AI\CLAUDE.md` 의 "🔑 `.env` 만드는 표준 절차".

**레지스트리 갱신(규칙)**: `C:\AI\CLAUDE.md` 3곳(표·점유 포트·추천 빈 포트) +
`C:\AI\인프라_도메인_Cloudflare_공유현황.md` §2.5.

---

## 8. 분기 절차 체크리스트

- [ ] **제출본 태그 먼저** — `git tag -a submit-2026-08-12 -m "회사 조별과제 제출본" && git push origin --tags`
- [ ] GitHub 본인 계정에 빈 private repo 생성
- [ ] `git clone` 으로 새 폴더 (복사+`git init` ✕ — **오늘 만든 파이프라인 이력이 날아간다**)
- [ ] `git remote set-url origin <새 repo>` → push
- [ ] compose 이름·포트 변경, Cloudflare 터널·서브도메인 신설
- [ ] NAS clone + `.env` + `compose up -d --build`
- [ ] 레지스트리 4곳 갱신
- [ ] 회사 설정 분리(§3) → 새 계열사 키워드 정의 → 사전필터로 후보 좁히고(§4) 판정

---

## 8.5 ✅ 원문 DB — **만들어 뒀다** (2026-08-12). 여기부터 쓰면 된다

**"할 때마다 파일 열어서 읽는" 걸 없애는 게 목적이었고, 그 도구가 이제 있다.**

```bash
node pipeline/build_corpus_db.mjs              # 원본 → C:\AI\_corpus\corpus.db (증분, 이미 넣은 파일은 건너뜀)
node pipeline/build_corpus_db.mjs --stats      # 현황만
node pipeline/corpus_query.mjs "반도체 AND 수출규제" --limit=20
node pipeline/corpus_query.mjs "수소" --in=minutes
node pipeline/corpus_query.mjs "HBM OR 고대역폭" --json=data/candidates_skhynix.json   # ← AI 판정 입력
```

| 파일 | 역할 |
|---|---|
| `pipeline/extract_hwpx.mjs` | HWPX → 문단 텍스트. **Node 내장만 사용(의존성 0)** — zip 직접 파싱 + `<hp:p>`/`<hp:t>` 추출 |
| `pipeline/lib/parse_speech_xlsx.mjs` | 발언 xlsx 파서(함정 3종 방어 포함). 수집기와 DB 빌더가 **공유** |
| `pipeline/build_corpus_db.mjs` | 원본 → SQLite(+FTS5). 증분 |
| `pipeline/corpus_query.mjs` | 전문검색 · `--json=` 으로 회사별 후보 추출 |

**DB 스키마** (`C:\AI\_corpus\corpus.db`, gitignore·재생성 가능)
```
speeches(id, src, conferNum, dae, classCode, committee, date, speaker, memberId, seq, text) + speeches_fts
minutes (id, src, meeting, para, text)                                                      + minutes_fts
ingested(file, kind, bytes, rows, at)   ← 증분 판단용
```
FTS5 문법: `A AND B` · `A OR B` · `A NOT B` · `"연속 구절"` · `접두사*`

### 📏 실측 수치 (2026-08-12) — 계열사 확장 비용을 가늠할 근거

| | 값 |
|---|---|
| 국회 발언 (중복 제거 후) | **약 103만 건** (수집 1,149,618 → 고유 1,034,540, **중복 10.0%** 제거) |
| 회의록 문단 | **142,625개** (hwpx 364개) |
| DB 크기 | 약 900MB~1GB |
| 위원회별 상위 | 법사위 131k · 과방위 125k · 산자위 92k · 행안위 78k · 농해수위 58k |

**키워드 사전필터 효과 — 이게 토큰 절감의 핵심 근거다:**

| 검색어 | 걸린 발언 | 전체 대비 |
|---|---|---|
| `반도체` | 2,065건 | 0.18% |
| `HBM OR 소부장` | 336건 | 0.03% |
| `주파수 OR 통신요금` | 135건 | 0.01% |
| `신약 OR 임상` | 227건 | 0.02% |

→ **계열사 하나 추가에 103만 건을 다 판정할 필요가 없다.** 키워드로 수백~수천 건까지 좁힌 뒤
그것만 AI 에 보내면 된다. 모델 등급을 낮추는 것보다 **이쪽이 훨씬 크게 먹힌다.**

⚠️ **다만 사전필터는 "재현율 우선, 정밀도는 AI 가"로 설계할 것.**
실측에서 `HBM OR 반도체클러스터 OR 소부장` 215건을 뽑았는데 1등 결과가 *공직복무점검단* 얘기였다.
FTS5 는 한국어를 형태소로 안 쪼개고(`unicode61`) 어절 단위로 매칭해서, 긴 문장 안에 스친 단어도 걸린다.
→ 사전필터는 **놓치지 않는 데** 집중하고(넉넉히 걸러라), 정밀도는 **AI 판정 단계의 `relevant:false`** 가 맡는다.
   기존 SK E&S 도 같은 구조였다 — 12,891건 중 7,764건(60%)이 판정에서 기각됐다. 그게 정상이다.
   ※ 정밀도를 더 올리고 싶으면 `"연속 구절"`(따옴표) 검색이나 `--committee=` 로 주력 상임위를 함께 거는 게 싸다.

### 🔴 데이터 범위 — **이 두 경계를 넘기지 말 것** (2026-08-12 사용자 지시)

분석 대상은 **"지금 정치인들의 생각"** 이다. 범위를 넓히면 정보가 느는 게 아니라 **오염된다.**

| 데이터 | 범위 | 근거·강제 방법 |
|---|---|---|
| 국회 상임위 발언 | **22대 국회만** (2024-05-30~) | 수집기가 서버 검색에 `facetDaeNum=22` + 시작일을 이중으로 건다 → **구조적으로 22대만** 들어온다. 실측 103만 건 전부 22대(다른 대수 0건). |
| 국무·차관회의 회의록 | **이재명 정부만** (2025-06-05~) | `build_corpus_db.mjs --minutes-since=2025-06-05`(기본값)이 그 이전 파일을 DB 에서 제외 |

**이재명 정부 경계를 2025-06-05 로 잡은 근거(실측):**
회의록 주재자 표기가 `250528` 까지 **대통령권한대행** → `250605` 부터 **대통령**.
⚠️ 국무총리로 자르면 안 된다 — 김민석 총리는 `250705` 부터고 그 전은 "직무대행"이라, **6월분 회의가 통째로 빠진다.**

- 원본 파일은 **지우지 않았다**(이미 받아둔 것이고 훗날 비교용이 될 수 있다). **DB 에만 안 넣는다.**
- 기존 대시보드 판정 데이터(`_cab_results.json`)는 2025-07-15~ 이라 **오염 0건**임을 확인했다.

### ⚠️ 알아둘 것
- **`.hwp`(구형) 122개는 DB 에 안 들어간다.** `.hwpx`(ZIP+XML)와 **완전히 다른 OLE 바이너리**라
  이 추출기로 못 읽는다. 전부 **2021~2022년**이고 PDF·hwpx 대체본이 없다.
  지금 분석 대상이 22대 국회(2024-06~)·이재명 정부(2025-07~)라 **의도적으로 제외**했다.
  필요해지면 별도 파서가 필요하다(난이도 있음).
- **PDF 회의록은 아직 DB 에 안 넣었다.** hwpx 로 대부분 커버되기 때문이다.
  넣으려면 기존 `extract_minutes.py`(pdfplumber) 결과를 `minutes` 테이블에 태우면 된다.
- `corpus.db` 는 **백업 대상이 아니다**(언제든 재생성). 백업할 건 `_corpus/*_raw/` **원본**이다.
- **중복 10% 가 있었다.** 같은 발언이 여러 xlsx 배치에 들어온다(위원회를 나눠 받으면 겹침).
  DB 에 유니크 인덱스(`conferNum, seq, speaker, 본문 앞 60자`)를 걸어 `INSERT OR IGNORE` 로 막았다.
  ⚠️ **판정 단계에서도 같은 원리를 유지할 것** — 안 그러면 같은 발언에 AI 비용을 두 번 낸다.

## 9. 아직 안 정해진 것

- **각 회사의 사업 카테고리** — 계열사 9개는 확정(§3.5), 카테고리는 SK E&S 8개만 확정이고 나머지 8개사는 초안.
- **새 repo 이름 / 서브도메인** — 미정.
- ~~원문 더미 전량 재수집~~ → **2026-08-12 완료**(국회 111만 건 · 회의록 466개). 다음은 §8.5 추출기·DB화.
- **`C:\AI\_corpus\` 백업 위치** — git 밖이라 이 PC 에만 있다(국회 111만 건 + 회의록 466개). 날리면 수 시간 재수집.
- **"부동산" 카테고리를 어느 계열사에 붙일지** — 보류 상태(2026-08-09 논의).
- `scheduler.mjs` 에 import 가드 씌우기 — §5-2 근본 차단책. 컨테이너 엔트리포인트라 배포 직후엔 안 건드림.

---

## 10. 규칙 (상위 `C:\AI\CLAUDE.md` — 자동으로 읽히지만 요약)

- **조사한 건 뭐든지 DB화.** 조사 전에 저장소부터 뒤진다. 못 찾은 것(`dead_ends`)도 남긴다.
- **커밋마다 `CHANGELOG.md`** 에 날짜·시각·요약. **실수·사고도 반드시.** 같은 커밋에 포함.
- **커밋 전 `/rule-check`** 로 절차 점검(코드 리뷰와 별개).
- **검증을 위임할 땐 내 결론을 알려주지 말 것.** 알려주면 검토자가 확인 대신 정당화만 한다.
  (2026-08-12 실제 효과: 죽은 코드 판정을 Fable 5에 백지로 넘겼더니 내 목록의 오류 2건이 잡혔다.)
- 새 포트·서브도메인은 **묻지 말고 알아서** 고르고 레지스트리를 갱신한다.
