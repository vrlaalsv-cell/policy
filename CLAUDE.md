@AGENTS.md

# policy — 정책돋보기 (SK E&S 판, 회사 조별과제 7조)

## 🔴 작업 시작 전에 반드시 읽을 것

**`HANDOFF_토큰절감_SKpolicy에서.md`** — 이 방에서 분기해 나간 `C:\AI\SKpolicy`(AI보좌관, SK 그룹사 판)에서
하루 동안 44M 토큰을 쓰며 실측·정정한 것들이다. 여기 적용해야 할 것이 6가지 있고,
그중 **§1 은 토큰 0으로 청와대 판정이 78→112회의가 된다**(호환성 실측 완료).

특히 아래 두 가지는 **규칙**이라 매번 지켜야 한다:

### ① 원본(PDF·엑셀·HWPX)은 최초 1회만 읽는다 — 그 뒤 모든 소비는 corpus.db 에서

> 사용자 지시(2026-08-14): "엑셀·PDF 원본은 최초 1회 json으로 DB화 하고 **다시 읽지 않는 것을 원칙**으로 하고,
> 모든 데이터는 최초에 json 같은 읽기 쉬운 것으로 DB화를 하고 **거기서만 읽는 것**으로 모든 경로를 바꿔줘."

```
[원본 PDF·엑셀·HWPX]  ──최초 1회──▶  [corpus.db]  ──▶  판정·검색·화면빌드
   _corpus/*_raw/                     여기서만 읽는다
```

⚠ **지금 이 방은 이 원칙을 안 지키고 있다**(2026-08-14 실측):
`cab_todo.mjs` 가 PDF 경로를 AI 에 넘기고, `collect_assembly_speeches.mjs` 는 엑셀을 **매 실행 전량 재파싱**한다.
`corpus.db` 를 만들어 놓고도 아무도 안 쓴다. 상세·고치는 법은 위 문서 §4.

### ② 토큰의 주범은 "에이전트 개수"다 — 파일 크기가 아니다

에이전트 1개당 고정비가 약 **66K 토큰**(시스템 프롬프트+도구정의+지시문+스키마)이라,
"항목 1개 = 에이전트 1개" 구조면 고정비가 항목 수만큼 곱해진다.
파일 형식을 PDF→텍스트로 바꾸는 건 **5.3%뿐**이었다(A/B 실측).
→ **줄이려면 에이전트 1개가 더 많은 일을 하게 하라.** 상세는 위 문서 §2·§3.

🔴 **"파일이 크니까 비싸겠지" 같은 직관으로 큰 결정을 내리지 말 것.** 같은 작업을 조건만 바꿔
2회 돌리고 `subagent_tokens` 를 비교하면 30초에 끝난다. SKpolicy 는 이 착각으로 "5배 절감"이라
잘못 보고했다가 A/B 에서 5.3%로 정정했다.

## 워크스페이스 공통 규칙

상위 `C:\AI\CLAUDE.md` 가 자동으로 읽힌다. 특히 §①-B(원본 1회 파싱)·§①-C(아끼려고 쓰는 사용량은 옳다)·
§①-D(에이전트 개수가 진짜 레버)가 이 방에 그대로 적용된다.

## 이 저장소 특성

- **6인 협업 repo**(`vrlaalsv-cell/policy`). 내 브랜치는 `minjeong`.
  커밋·푸시 전에 `git status`·`git log` 로 팀 변경을 먼저 확인하고, 가능하면 팀에 알릴 것.
- 배포: NAS 8007 · Cloudflare 터널 **7.yes-i-can.kr** (조 번호 = 서브도메인)
- 원문 코퍼스는 `C:\AI\_corpus` 를 `SKpolicy` 와 **공유**한다(git 밖). 규칙은 `_corpus/README.md`.
