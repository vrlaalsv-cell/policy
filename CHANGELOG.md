# CHANGELOG

의미 있는 변경이 있을 때마다 날짜·시간(KST)과 주요 내용을 남긴다. 사소한 수정까지 전부 적진 않는다 — 그런 건 `git log`가 이미 갖고 있다. 여기는 "무엇이·왜 바뀌었는지" 나중에 훑어보기 위한 요약이다.

이 파일은 2026-08-09부터 시작한다. 그 이전 이력은 `git log`로 확인.

---

## 2026-08-29

### 12:16 — 기사·AI 분석 전면 최신화 + "근거가 바뀐 사람만 다시 돌리는" 증분 구조 신설

**기사 확인 결과 — 사이트는 이미 최신이었다.** NAS 스케줄러(하루 5번)가 계속 돌아서
라이브 기사는 **486건 · 오늘(8/29)까지 · 발췌 99.6% · 원문링크 92% · 언론사·라벨 100%** 였다.
낡은 건 기사가 아니라 **AI 분석**이었다 — 8/8~8/16 기사 기준으로 만들어진 채였다.
로컬도 재수집해 맞췄다(349명 조회, 479건, 발췌 477건). 군사 '전력(戰力)' 오탐 6건은 자동 제외.

**🔴 핵심 — "전부 최신화"를 전원 재생성으로 하지 않았다.**
근거가 하나도 안 바뀐 사람까지 다시 돌리면 그대로 토큰 낭비다(워크스페이스 §① "한 거 또 하지 마라").
그래서 **인물별 근거 해시(srcHash)** 를 도입했다 — 프롬프트에 실제로 들어가는 payload
(성향·발언·기사)를 그대로 해시해 `_ai_results.json` 에 같이 저장하고, 다음 실행 때 지금 해시와 비교한다.
같으면 다시 돌려도 **같은 답이 나오므로 건너뛴다.** 판단 기준이 "기사가 갱신됐나"가 아니라
**"이 사람에게 들어가는 근거가 달라졌나"** 인 것이 요점이다 — 기사는 하루 5번 갱신되지만
사람의 성향이 하루 다섯 번 바뀌지는 않는다.

- `build_ai_input.mjs`: **`--todo`** 신설(없는 사람 + 근거 바뀐 사람). 사유별 내역도 출력한다.
- `collect_ai.mjs`: 결과에 `srcHash`·`generatedAt` 을 같이 기록. 이게 없으면 다음 실행이 전원을 또 돌린다.
- 기존 281명은 해시가 없어 그대로면 전원이 대상이 됐을 것이다 → **생성 당시 스냅샷(8/09·8/12·8/16·8/27)
  으로 되돌려 payload 를 재구성**해 비교했고, 그대로인 **149명에는 재생성 없이 해시만 backfill**(토큰 0).
  표본 검증으로 기사 목록이 실제로 동일함을 확인했다.

**결과: 279명 중 149명 건너뛰고 130명만 재생성** (국회 106 + 청와대 24, 17배치).
전원 재생성 대비 약 **절반의 토큰**으로 끝났다. 실측 47 에이전트 · 4.09M 토큰 · 9.5분 · 실패 0.

**🔴 근거 검증에서 130명 중 17명(13%)이 탈락해 재생성됐다.** 검증 없이 넣었으면 그대로 실렸을 오류들:
- `M091` — 발언 건수를 "8건 중 5건"으로 틀리게 적고, 원문엔 없는 **암모니아**를 연료로 추가
- `M108` — **다른 의원(M109)의 논점**(주민 수용성)을 가져다 붙임
- `M216` — 기사엔 "태양광"뿐인데 **"수면(부유식) 태양광"** 으로 구체화
- `M170` — 원문은 "10%에서 0.32%로 떨어졌다"뿐인데 그 수치를 **"지분율"** 로 특정
- `M036`·`M068`·`M080` — stances 가 "자료없음"인 사업에 성향을 단정
이런 종류는 그럴듯해서 사람 눈으로 안 걸린다. `wf_ai_verified.js`(8/27 신설)를 쓴 이유가 이것이다.

**근거가 사라진 사람에게는 총평을 안 붙인다** (`build_ai.mjs`, 4명: 홍기원·이광재·허영·조계원).
기사는 365일 창이라 시간이 지나면 빠지고 오탐도 나중에 걸러진다 → **화면엔 근거가 하나도 없는데
"AI 종합 분석"만 떠 있는 상태**가 되어 독자가 검증할 수 없다. ⚠ `_ai_results.json` 에서 지우지는
않는다 — 기사가 다시 잡히면 되살아나야 하고, 지우면 재생성 토큰을 또 쓴다. **표시 단계에서만** 거른다.

해시 backfill 에 쓴 마이그레이션 스크립트는 **`pipeline/stale_audit.mjs` 로 커밋**했다(rule-check 지적 반영).
평소엔 쓸 일이 없지만, `build_ai_input.mjs` 의 payload 조립이 바뀌면 저장된 해시가 **전부 무효**가 되어
같은 상황이 재현되므로 남겨 둔다. 이미 해시가 있는 사람은 대상에서 빼도록 해 오해를 막았다.
`AUTOMATION_TODO.md` §B "만들 것"(API 이식 스펙)도 `wf_ai_verified.js` 기준으로 고쳤다 —
예전 스펙대로 옮기면 **검증 게이트가 조용히 빠진다.**

**최종**: 국회 230/300명 · 청와대 49/49명(전원) · `_ai_results.json` 284명(해시 279명).
`--todo` 재실행 시 **0건** — 증분 루프가 닫혔다.
(국회 70명은 총평이 없는 게 정상이다 — 에너지 발언 0건 + 기사 0건이라 근거가 없다. 보건복지위·교육위 등
에너지와 접점 없는 상임위 소속이 대부분이고, 억지로 채우면 위 17건 같은 추측성 분석이 된다.)

## 2026-08-27

### 16:07 — 🔴 청와대 AI 총평이 13일간 통째로 빠져 있던 것 발견·복구 (49명 전원)

화면에 "국회의원은 AI 총평이 있는데 청와대는 없다"는 지적에서 출발했다. 확인해 보니
`web/cabinet.js` 의 `speakers[].ai` 가 **0명**이었다 — 발언 발췌 309건만 있고 인물별 총평이 전무.

**원인 — 재빌드가 총평을 조용히 지운다.** `build_cabinet2.mjs` 는 `speakers` 배열을 통째로
새로 만든다. 8/14 에 회의록 78→112회의를 반영하며 이걸 돌렸는데 **`build_ai.mjs` 를 이어서 안 돌렸다.**
그 순간 이미 있던 청와대 총평 33명분이 사라졌고, **에러도 경고도 없어서 13일간 아무도 몰랐다.**
(`AUTOMATION_TODO.md` "⚠️ 함정" #2·#3 이 경고하던 바로 그 위험이 실제로 터진 것이다.)

**먼저 SKpolicy 에서 가져올 수 있는지 확인했다 — 없었다.** (요청: "SKpolicy 에서 가져올 수 있으면
가져오고, 없으면 새로 분석") NAS `skpolicy.종료_20260824/data/` 의 접미사 없는 `_ai_results.json`(=SK E&S 몫)은
policy 쪽과 **크기·cab 키 수가 동일한 같은 파일**이고, 나머지 `_ai_results_*.json` 9개는 계열사(skax·skgas·
skt 등) 관점이라 SK E&S 8사업에 못 쓴다. SKpolicy 는 애초에 `COMPANY_IDS.filter(id => id !== "skens")` 로
**SK E&S 를 제외하고** 돌리기 때문에 그쪽엔 신규 산출물이 원천적으로 안 생긴다.
→ `data/findings.json` 의 `dead_ends` 에 기록했다(다음 세션이 NAS 아카이브를 또 뒤지지 않도록).

**고친 것 — 재발 방지가 핵심이다. 데이터만 채우면 다음 재빌드에 또 날아간다.**
- **`build_cabinet2.mjs`**: 재빌드 시 기존 `cabinet.js` 의 `speakers[].ai` 를 **이름 기준으로 물려받는다.**
  ai 는 발언이 아니라 인물에 붙는 값이라 재빌드와 무관하게 유효하다. (검증: 재빌드 후 33/33 보존)
- **`build_ai.mjs`**: **대량 실패 가드** 신설. 지금 붙어 있는 ai 수보다 이번에 붙일 게 60% 미만이면
  아무것도 쓰지 않고 `exit 1`(강행은 `--force`). 입력이 깨졌을 때 246명분을 날리던 위험을 막는다.
  붙지 못한 고아 key 도 이제 경고로 출력한다(예전엔 조용히 누락).
- **`collect_ai.mjs`**: **덮어쓰기 → key 기준 증분 병합.** 예전 버전으로 이번 23명분을 수집했다면
  기존 258명분이 **전부 날아갈 뻔했다.** 빈 `analysis` 결과는 무시해 실패 배치가 멀쩡한 총평을 덮지 않게 했다.
- **`build_ai_input.mjs`**: `--missing` 증분 플래그 신설(+`--batch=N`). 총평이 이미 있는 사람은 빼고
  남은 사람만 배치로 만든다. 이번에 청와대 49명 중 **33명을 건너뛰고 16명만** 보냈다.
  0건이면 "AI 단계를 돌릴 필요 없다"고 출력한다(워크스페이스 §① "한 거 또 하지 마라").

**발언자 이름 오염 6건 정규화 → 중복 인물 5명 병합 (55명 → 49명).**
AI 발췌가 `"대통령 이재명"`·`"국무총리 김민석"` 처럼 직위를 이름 앞에 붙여 뱉은 값이 있었는데,
`bySpeaker` 가 문자열을 그대로 키로 써서 **같은 사람이 두 명으로 집계**되고 있었다. 쪼개진 쪽은
발언수가 1~3건이라 목록 뒤로 밀렸고, `cab:<이름>` 키가 안 맞아 **총평도 안 붙었다.**
자르는 규칙은 통념이 아니라 데이터 기반이다 — 오염된 값 6건은 **전부 자기 `role` 로 시작**한다.
(이름 길이로 자르는 방식은 안 썼다. 남궁·선우 같은 복성이면 4글자라 오작동한다.)
결과: 김성환 56→59건 · 이재명 51→53건 · 조원철 24→27건으로 합쳐짐.

**총평 생성 — `pipeline/wf_ai_verified.js` 신설 (워크플로 7 에이전트 / 585K 토큰 / 4분).**
남은 23명(청와대 16 + 국회 7)을 3배치로 묶어 생성하고, **근거 기반 적대 검증**을 붙였다.
이들은 발언이 1~2건뿐이라 AI 가 근거 없이 부풀릴 위험이 큰 구간이기 때문이다. 실제로 **2건이 탈락**했다:
- `M215` — 기사 제목엔 "에너지고속도로 지중화"뿐인데 "재생에너지 송전망"·"주민 수용성" 까지 외부
  지식을 끌어와 "추진에 적극"이라 단정(입력 stances 는 8사업 전부 자료없음).
- `M225` — stances 가 원전:자료없음인데 "원전에 강한 우호"라 단정하고, 근거가 기사 제목 3건뿐인데도
  판단 근거가 제한적이라는 취지를 안 밝힘.
둘 다 재생성해 "성향 판단 불가"로 정직하게 바뀌었다. **검증 없이 넣었으면 틀린 분석이 화면에 실렸다.**
검증 단계가 붙은 이 워크플로를 `pipeline/wf_ai_verified.js` 로 커밋했다 — 기존 `wf_ai.js` 는 생성만 하고
그대로 믿기 때문에, **근거가 얇은 인물(발언 1~2건)을 돌릴 땐 검증 붙은 쪽을 쓸 것.**
`build_ai_input.mjs --missing` 의 출력 힌트도 이쪽을 가리키게 바꿨다.

⚠️ **남은 것**: NAS 폴더명이 `policy.종료_20260824` 로 바뀐 채라 **autodeploy 가 멈춰 있다.**
푸시해도 자동 반영되지 않으니 폴더명을 되돌려야 한다(`AUTOMATION_TODO.md` §F 에 과제로 남김).

**결과**: 청와대 **49명 전원** 총평(33→49) · 국회 224→231명 · `_ai_results.json` 258→281명.
`source` 문구도 정정 — 예전엔 발췌가 나온 77회만 적어 **분석 범위가 실제의 69% 로 축소돼 보였다.**
이제 "112회 전량 분석(에너지 발언이 있는 77회에서 발췌)"로 둘 다 적는다.

### 15:49 — 사이트 복구 — 8/24 종료 작업 때 내려간 뒤 3일간 죽어 있었다

`7.yes-i-can.kr` 이 **Cloudflare Error 1033** 이었다. 원인은 장애가 아니라 8/24 프로젝트 종료 작업이다:
컨테이너 3개가 `compose down` 되고 NAS 폴더가 `policy` → `policy.종료_20260824` 로 바뀌면서
터널이 연결 대상을 잃었다. NAS 재부팅은 아니다(26일째 무중단, 다른 프로젝트 컨테이너는 전부 정상).

- 남아 있던 이미지·`.env` 를 그대로 재사용해 **재빌드 없이 10초** 만에 복구
  (`compose -p policy up -d` — **`-p policy` 필수**. 폴더명이 바뀌어 기본 프로젝트명이 달라졌기 때문에
  안 붙이면 `policy-app:latest` 를 못 찾고 J4125 로 5~10분 재빌드한다).
- 스케줄러가 뜨자마자 자동 수집을 시작했다(데이터가 76시간 낡음 → 즉시 1회). 뉴스는 Google RSS 라 무료.
- ⚠️ **autodeploy 도 같이 죽어 있었다** — `scripts/autodeploy*.sh` 가 `/volume2/docker/policy` 를
  하드코딩하는데 폴더명이 바뀌어서다. 폴더명을 되돌려야 자동배포가 살아난다.
- 교훈: **"이제 개발 안 한다"는 배포를 내리라는 뜻이 아니다.** 종료 작업에 `compose down`·폴더명 변경을
  묶지 말 것 — 이 프로젝트는 Claude API 를 안 써서 띄워두는 비용이 사실상 0인데, 내려서 얻은 건 없고
  살아있던 공개 서비스만 잃었다.

## 2026-08-16

### 23:06 — 뉴스 재수집(오늘 날짜로 갱신) — AI 총평은 스킵

"전부 오늘 날짜로 갱신" 요청에 따라 SKpolicy 방 오늘 작업(청와대 판정·국회 발언 9개사 완료,
뉴스 9개사 정리)을 먼저 점검했다.

- **청와대 회의록·국회 발언**: `cab_todo.mjs` 확인 결과 DB 112회의 전부 이미 판정 완료(미판정 0),
  `_corpus/cabinet_raw` 도 8/12 이후 새 파일 없음 — SKpolicy 가 "추가로 받았다"고 한 회의록은
  전량 8/14에 이미 이 방에 이관된 것(78→112)과 같은 것이었다. **새로 반영할 회의록 없음.**
- **워크플로 크래시 버그**(SKpolicy 오늘 `.catch()` 방어 추가분): policy 의 `wf_ai.js`·`wf_board.js`·
  `wf_cabinet.js` 는 애초에 `filter(x => x.ok)` 가 아니라 `filter(Boolean)`·실제 성향 재확정
  패턴이라 SKpolicy 가 겪은 크래시(신규 `.ok` 필드 접근 중 null 역참조)에 해당하지 않음을 확인.
  **포팅 불필요**(SKpolicy 의 버그는 자기 리팩터링 중 새로 생긴 것).
- **뉴스**: `data/news.json` 이 8/8 이후 정체돼 있어 `5_collect_news.mjs`(Google RSS, 무료) +
  `6_fetch_excerpts.mjs` 재실행 — 465건 수집, 발췌 457건, `web/news.js` 재생성.
- **AI 총평 재생성은 스킵**: 새 뉴스 반영 시 49배치(국회의원 230명+청와대 55명) 규모의 Claude API
  호출이 필요했으나, 사용자 지시로 이 프로젝트의 API 사용량은 **출품(최종 제출) 시점까지 아껴둔다**
  — 평소 갱신 때는 쓰지 않는다. 기존 총평 문구는 8/8 시점 그대로 유지.

## 2026-08-14

### 23:15 — 청와대 파이프라인 코드 3종 — SKpolicy 검증 패턴 이식 (§4+§5)

`HANDOFF_토큰절감_SKpolicy에서.md` §4(원본→DB 전환)·§5(실패/성공 오판정 버그) 를 문서 안내에
그치지 않고 **코드로 실제 적용**했다. SKpolicy 방에서 먼저 겪고 고친 것과 동일한 패턴.

- **`collect_cab.mjs` — 가장 급했던 버그**: 예전엔 `_cab_results.json` 을 실행마다 **통째로
  덮어썼다**(judgedMeetings·statements 보존 없음). 방금 이관한 112회의 판정이 다음 실행 한 번에
  날아갈 수 있는 상태였다. → 기존 파일을 스프레드로 이어받아 **증분 병합**(같은 회의 재판정 시엔
  옛 발췌만 교체)하도록 재작성. `company`·`configHash`·`runs` 등 몰라도 되는 필드도 안 지운다.
- **`wf_cabinet.js` — 실패/성공-0건 구분**: `results.filter(Boolean)` 은 에이전트 실패(null)와
  "에너지 발언 없음" 0건 성공([])을 구분 못 한다(`.then()`이 항상 배열을 반환하므로). `ok` 플래그로
  분리한 `okMeetings` 를 반환하도록 수정 + 원본 파일 경로(pdf·hwpx 등) 유입 방어(throw) 추가 +
  첫 요청 혼자 보내고 나머지 병렬(프롬프트 캐싱 규칙 ②) 적용.
- **`cab_todo.mjs` — 원본 재읽기 제거**: PDF 경로를 그대로 넘기던 걸 `corpus.db` 의 `minutes`
  테이블에서 회의별 텍스트를 뽑아 `data/_cabtext/*.txt` 로 캐시해 넘기도록 전면 재작성.
  `--limit=` 시간축 균등 표집도 이식. 실행 결과: DB 112회의 · 이미 판정 112 · 미판정 0(정합 확인).
- **`update-cabinet.mjs` — 자동 DB 적재 연결**: 새 회의록을 내려받은 뒤(`ok > 0`) 자동으로
  `extract_pdf_text.py` → `build_corpus_db.mjs` 를 이어 부르도록 추가. 예전엔 다운로드만 하고
  끝나서 사람이 안 돌리면 DB 가 조용히 낡았다.
- `.gitignore` 에 `data/_cabtext/`(corpus.db 파생 텍스트, 재생성 가능) 추가.
- 검증: 4개 파일 전부 `node --check` 문법 통과 + `collect_cab.mjs` 를 더미 워크플로 출력으로
  실제 실행해 112→113 증분 병합 확인 후 `git checkout` 으로 원상복구(운영 데이터 안 건드림).
- 범위 밖으로 남긴 것: `collect_assembly_speeches.mjs` 의 xlsx 매 실행 전량 재파싱은 **AI 토큰이
  아니라 로컬 CPU 비용**이라(다운로드는 이미 증분) 이번엔 안 건드림 — 필요하면 별도 요청.

### 22:30 — ✅ 청와대 판정 실제 적용 — 78 → 112회의 (토큰 0)

위 22:10 문서에서 안내한 §1 을 **실제로 적용**했다. `SKpolicy` 가 SK E&S 관점으로 완주한
112/112회의 판정을 그대로 가져왔다 — 재판정 없이 파일 복사만, **AI 호출 0회**.

| | 이전 | 지금 |
|---|---:|---:|
| 판정 회의 | 78 | **112** |
| 발췌 발언 | 194 | **309** |
| 발언자 | 34 | **55** |

사업별 분포: POWER 118 · RE 143 · LNG 50 · NUCLEAR 44 · DISTE 25 · ESOL 24 · H2 14 · CITYGAS 8
(성향 favor 202 · neutral 96 · oppose 11). **0건 카테고리 없음.**

- 복사 전 `data/_cab_results.json.bak-20260814` 로 백업(gitignore 대상 — 로컬에만 둔다).
  `.gitignore` 에 `*.bak-*` 패턴 추가(기존 `*.bak` 은 날짜 붙은 이름을 못 걸렀다).
- `build_cabinet2.mjs` 재실행으로 `cabinet.json`·`web/cabinet.js` 갱신. 무결성 확인
  (`window.CABINET_DATA` 대입 정상 · 파싱 정상 · 0건 카테고리 없음).
- ℹ️ 화면 문구가 **"회의록(77회)"** 로 나오는데 버그가 아니다 — **112회의를 판정했고 그중
  77회의에서 에너지 관련 발언이 나왔다**(35회의는 관련 발언 0건). `source` 는 발언이 나온
  회의만 세는 기존 동작이고 SKpolicy 도 같다.

**국회·기사 데이터는 손대지 않았다 — 대조해보니 이미 동일했다**:
국회 판정 아카이브 12,891건이 key 기준으로 **완전히 일치**(판정 내용 다른 것 0건),
`web/data.js` 도 의원 300명·인용 4,712건·AI총평 224명으로 같다. 기사도 같은 수집분(2026-08-08).
차이는 SKpolicy 쪽에 `configHash` 메타 필드가 더 있는 것뿐이라 옮길 이유가 없다.

### 22:10 — SKpolicy(그룹사 판)에서 토큰절감 지침 이식 + 청와대 판정 물려받기 안내

이 방에서 분기해 나간 `C:\AI\SKpolicy` 가 하루 동안 44M 토큰을 쓰며 실측·정정한 것들을
이 방에 적용할 수 있게 정리해 넘겨받았다. **문서만 추가했고 코드·데이터는 아직 안 건드렸다.**

- **`HANDOFF_토큰절감_SKpolicy에서.md` 신설** — 8개 절, 우선순위 표 포함.
- **`CLAUDE.md` 확장**(기존 `@AGENTS.md` 한 줄 → 세션 시작 시 읽히는 지침) — 위 문서 포인터와
  핵심 규칙 2개(원본 1회 파싱 · 에이전트 개수가 토큰 주범)를 박았다.

**바로 얻을 수 있는 것 — 청와대 판정 78 → 112회의, 토큰 0.**
SKpolicy 가 SK E&S 관점으로 112/112 전량을 완주했고, 이 방과 **호환성을 실측 확인**했다
(발언 필드 7개 동일 · 사업ID 8개 동일). `data/_cab_results.json` 을 복사하면 발언 194→309건이 된다.

**이 방에서 확인된 미적용 항목**(2026-08-14 실측):
- `cab_todo.mjs` 가 corpus.db 대신 **PDF 원본 경로**를 AI 에 넘긴다.
- `collect_assembly_speeches.mjs` 가 엑셀을 **매 실행 전량 재파싱**한다(L237) — 다운로드는 증분인데 파싱은 아니다.
- `update-cabinet.mjs` 가 다운로드만 하고 추출·DB적재를 안 불러 **DB 가 조용히 낡는다**.
- `wf_cabinet.js` 가 에이전트 실패와 성공-0건을 구분 못 해 **실패가 "판정 완료"로 기록될 수 있다**(SKpolicy 실사고).
- `corpus.db` 를 만들어 놓고 소비하는 곳이 없다.

**SKpolicy 가 틀렸다가 정정한 것도 같이 적어 뒀다** — "PDF 를 읽어서 5배 비싸다"는 진단이
A/B 실측에서 **5.3%**로 판명났다. 진짜 주범은 에이전트 1개당 고정비 66K 였다.
같은 착각을 반복하지 않도록 문서 §2 에 경위까지 남겼다.

## 2026-08-12

### 21:00 — ✅ 코퍼스 최종 완성 + 공용 폴더 구조 정리
- **최종 규모**: 국회 발언 **1,117,493건**(22대 전량) · 회의록 **67,789문단 / 112회의**(이재명 정부 전량).
  이전 정권 오염 **0건**. 전문검색 정상 동작 확인.
- PDF 추출 102개 전부 성공(83,275문단). **2단 조판이 2,002페이지 중 1,338페이지(67%)** —
  검증된 거터 판별 로직을 그대로 가져오길 잘했다. 그냥 뽑았으면 3분의 2가 좌우 뒤섞였을 것.
- 회의록 커버리지: HWPX 85개 + PDF 보충 17개 = **112회의 전부**.
- 📁 **공용 폴더 구조 정리**(사용자 지적: "추출된 파일은 공용에 놔야 다른 프로젝트에서 쓰지"):
  `_corpus/{assembly_raw,cabinet_raw}` = 원본·백업 대상 / `_corpus/{extracted,corpus.db}` = 파생·재생성 가능.
  `_corpus/README.md` 에 구조·만드는 법·범위 규칙을 정리하고 `C:\AI\CLAUDE.md` 표도 원본/파생 구분으로 갱신.

### 20:30 — 🗑️ 이전 정권 회의록 전량 삭제 + 재수집 차단 + PDF 경로 추가
- 사용자 지시: **"이전 정권꺼는 다 지워줘. 헷갈리니까"**
- 삭제: **476개**(2021-01-05~2025-05-28) + **연도별 묶음 zip 36개**(2003~2020, 197MB).
  zip 은 파일명에 날짜 패턴이 없어 1차 삭제에서 빠져 있었다 — 목록을 눈으로 확인해서 잡았다.
  남은 건 **112회의(2025-06-05~2026-06-23) 전부 이재명 정부**.
- 🔒 **재수집 차단** — `update-cabinet.mjs` 에 `--floor`(기본 2025-06-05) 추가.
  안 막으면 **매일 05:30 스케줄러가 지운 걸 도로 받아온다.** 회의일을 못 읽는 글(묶음 zip 등)도 함께 제외.
  인덱스에서도 이전 정권 338건 제거. `--all --pages=45` 로 검증: 441건 중 339건 제외, 받을 대상 102건.
- **PDF 회의록도 코퍼스에 넣는 경로 추가** — HWPX 커버리지가 95/112(85%)라 나머지는 PDF 가 필요하다.
  `pipeline/extract_pdf_text.py` 신설(**무필터**, 2단 조판 처리는 검증된 로직 그대로 이식).
  🔴 `extract_minutes.py` 는 **에너지 키워드로 걸러서 SK E&S 전용**이라 코퍼스에 쓰면 반도체·통신·제약이 날아간다.
- 부수: `--all` 재수집으로 각 회의가 **PDF+HWPX 둘 다** 확보됨(112→197파일). HWPX 쪽이 추출 품질이 좋다.

### 19:30 — 원문 DB화 완료 — HWPX 추출기 + SQLite(FTS5) + 검색 도구
- 목적: **"할 때마다 파일 열어서 읽는" 걸 없앤다**(사용자 지시). 원본 확보 → DB화 → 활용의 2단계.
- `pipeline/extract_hwpx.mjs` — HWPX → 문단 텍스트, **Node 내장만 사용(의존성 0)**.
  zip 중앙디렉터리를 직접 파싱하고 `<hp:p>`(문단) 안의 `<hp:t>`(텍스트런)을 이어붙인다.
  ⚠ 한 문단이 서식 단위로 여러 `<hp:t>` 로 쪼개져 있어 **문단 단위로 합쳐야** 말이 된다.
  파이썬 대조 검증: **문단 320개 정확히 일치**(글자수 14자 차는 이쪽이 HTML 엔티티를 디코딩해서 나는 정상 차이).
- `pipeline/lib/parse_speech_xlsx.mjs` — 발언 xlsx 파서를 공용 모듈로 분리.
  ⚠ 수집기를 `import` 하면 **수집이 실제로 실행되므로**(오늘 스케줄러에서 당한 그 함정) 파서만 떼어냈다.
- `pipeline/build_corpus_db.mjs` — 원본 → `C:\AI\_corpus\corpus.db`(SQLite+FTS5). **증분**(`ingested` 대장).
  `node:sqlite` 가 Node 22.5+ 내장이라 **npm 설치·네이티브 빌드 불필요** → NAS 에서도 그대로 돈다.
- `pipeline/corpus_query.mjs` — 전문검색. `--json=` 으로 **회사별 후보를 뽑아 AI 판정 입력**으로 쓴다.
  이게 그룹사 확장의 핵심 도구다(서버 재요청 0 · 판정 대상만 좁혀 토큰 절감).
- ⚠️ **`.hwp`(구형) 122개는 제외**했다. `.hwpx` 와 달리 OLE 바이너리라 포맷이 완전히 다르다.
  전부 2021~2022년이고 대체본이 없는데, 분석 대상이 22대 국회(2024-06~)라 우선순위가 낮다.
  (처음엔 정규식 `\.hwpx?$` 로 둘을 같이 잡아 전량 실패했다 — 확장자 하나 차이로 포맷이 다르다.)
- 🔴 **중복 10% 발견·제거.** 같은 발언이 여러 xlsx 배치에 들어온다(위원회를 나눠 받으면 겹침).
  실측 1,149,618건 → 고유 **1,034,540건**. 그대로 뒀으면 AI 판정 비용이 10% 그냥 샜다.
  DB 에 유니크 인덱스(`conferNum, seq, speaker, 본문 앞 60자`) + `INSERT OR IGNORE` 로 차단, 재빌드 후 중복 0.
- **최종 규모**: 발언 **103만 4,540건** · 회의록 **142,625문단** · DB 966MB · 실패 0.
  키워드 사전필터 효과 실측 — `반도체` 1,183건(0.11%) · `HBM OR 소부장` · `주파수 OR 통신요금` 등
  전부 수백~수천 건대. **계열사 하나 추가에 103만 건을 다 판정할 필요가 없다**는 근거.
- 수집기에도 **재시도/백오프 추가** — 111만 건을 몇 시간 받다가 95만 지점에서 ECONNRESET 으로
  프로세스가 죽었다. 이제 일시적 통신 오류는 넘기고 이어간다(이미 받은 파일은 건너뛰므로 재개가 싸다).

### 18:10 — ❌ 정정: "행안부가 옛 회의록을 내린다"는 **오판이었다** (원인은 WAF 레이트리밋)
- **앞선 17:30 항목의 결론이 틀렸다.** 파일은 전부 살아 있다 — 게시판에 441건(45페이지)이 그대로 있고,
  "못 받는다"던 2025-07-15 회의록을 **단건으로 받으니 1.1MB 정상 다운로드**됐다.
- 진짜 원인: **행안부 WAF 가 연속 요청을 차단한다(HTTP 400).** `--all` 실행 때 앞의 42건은 성공하고
  그 뒤 108건이 전부 실패했는데, 목록이 최신순이라 **"과거분만 실패"처럼 보여** 삭제로 오판했다.
- 조치: 요청 간 간격(기본 1.5초) + 400/403/429/503 백오프 재시도(3s→6s→12s→24s).
  → `--pages=12` 재실행에서 **신규 74건 · 실패 0건**, 보유 46 → 120건. 이어서 전량 수집 중(2023년까지 내려감).
- 🔴 **판별법을 findings.json 에 남겼다**: 대량 수집 중 실패하면 **그 파일을 단건으로 다시 받아 볼 것.**
  받아지면 레이트리밋이고, 그래도 400 이면 그때 삭제를 의심한다. (dead_ends 에서 빼고 '해결된 함정'으로 이동)
- **hwpx 도 함께 받도록 변경.** 2025-07-05 이전 글은 **hwpx 만 있고 pdf 가 없어서** pdf 만 받으면
  그 구간을 통째로 놓친다. 게다가 hwpx 가 추출에 유리하다 — ZIP+XML 이라 문단 순서가 그대로고
  (PDF 는 2단 조판에서 좌우가 뒤섞여 방어 코드가 필요했다), 10배 작고, **Node 만으로 파싱된다**
  (컨테이너에 파이썬을 넣어야 하는 숙제가 사라진다). 실측 추출 9,385자 정상.

### 17:40 — 스케줄러에 회의록 PDF 자동 확보 추가
- `policy-scheduler` 컨테이너가 **매일 05:30 KST + 컨테이너 시작 시** 행안부 회의록을 훑어 새 PDF 를 받는다.
  이유는 바로 아래 17:30 항목 — **놓치면 영구 소실**이라 사람 기억에 맡길 수 없다.
- **받기만 한다.** AI 판정·빌드는 사람이 PC 에서(비용 단계라 일부러 자동화 안 함).
- PDF 는 `./runtime/cabinet_raw/`(볼륨)에 쌓여 재빌드해도 남는다. `CABINET_FETCH=0` 으로 끌 수 있다.
- 실패해도 기사 수집 루프를 죽이지 않는다(다음 주기 재시도).
- 부수 수정: 원본 경로 기본값이 윈도우 절대경로라 **컨테이너(리눅스)에서 깨질 뻔한 것**을 고쳤다 —
  공용 폴더가 실제로 있을 때만 쓰고, 없으면 프로젝트 `data/` 로 떨어진다.
- 실기동 테스트로 "시작 시 확인 → 신규 0건 → 다음 작업 대기" 로그까지 확인.
- ⚠️ NAS 가 받은 PDF 는 판정하려면 PC 로 내려받아야 한다(판정은 PC 에서 함). DEPLOY.md 에 명시.

### 17:30 — 원본(raw) 공용 보관소 신설 + 🔴 행안부 원본이 시간 지나면 사라진다는 발견
- **원본을 `C:\AI\_corpus\` 로 분리**(워크스페이스 공용, git 밖). 국회 발언 xlsx 121개(34MB)와
  국무회의 PDF 46개(59MB)를 프로젝트 폴더에서 옮겼다. 그룹사 버전 등 다른 방이 **재수집 없이 같이 쓴다.**
  경로는 `--raw-dir=`/`--dir=`(env `SPEECH_RAW_DIR`/`CABINET_RAW_DIR`)로 설정화 — 하드코딩 안 함.
- `collect_assembly_speeches.mjs` **`--raw-only` 모드 추가** — 무필터로 원본만 받고
  `assembly_speeches.json`(SK E&S 산출물)은 안 건드린다. 제출본 데이터를 지키면서 원본을 모을 수 있다.
  22대 상임위 **17개 전량** 수집 시작(백그라운드, 수 시간).
  ※ 배경: 기존 12,891건은 에너지 키워드로 걸러진 결과라 **원본의 5.4%** 뿐 — 반도체·통신·제약엔 못 쓴다.
- 🔴 **행안부 회의록은 시간이 지나면 다시 못 받는다.** 08-09에는 2026-01-23부터 받아졌는데
  08-12에는 **2026-01-23~02-03 4건이 새로 HTTP 400**. 3일 만에 4건이 추가로 사라졌다.
  → 차단 구간이 앞으로 밀린다. **받을 수 있을 때 받아 두고 원본을 절대 지우지 말 것.** findings.json 갱신.
- 🐛 **`update-cabinet.mjs --all` 이 인덱스를 파괴하던 버그 수정.** `--all` 은 "인덱스를 무시하고 다시
  받아본다"는 뜻인데 인덱스를 **빈 값에서 다시 쓰는** 구조라, 이번 실행에서 못 받은 과거분이
  **PDF 는 손에 있는데도 인덱스에서 통째로 사라졌다**(실측 4건 유실 → 복구). 이제 파일이 남아 있으면 항상 보존한다.
- `HANDOFF_그룹사.md` 신설 — 새 세션이 그룹사 버전을 이어받기 위한 인수인계(계열사 9개·설계 이슈·함정·절차).

### 16:20 — 죽은 코드 15개 삭제 + AI 재판정 낭비 구조 제거 + 프롬프트 캐싱
- **죽은 스크립트 15개 삭제.** Fable 5 에게 **내 판정을 알려주지 않고** 독립 검증시킨 뒤 삭제
  (`_extract_journal`·`build_assembly2`·`build_assembly_analysis`·`build_board_input`·`build_cabinet`·
  `build_from_fetch`·`build_profiles`·`cab_filelist`·`collect_ab2`·`extract_excerpts`·`find_untagged`·
  `parse_speeches`·`parse_speeches_ctx`·`split_batches`·`split_batches2`).
  검증에서 내 목록이 두 군데 틀린 게 잡혔다 — `collect_cab.mjs` 는 `wf_cabinet.js` 의 짝 수거기라 **보존**
  (지웠으면 청와대 재분석 경로가 고아가 될 뻔), `build_board_input.mjs` 는 문서가 "쓰지 말 것"이라 **삭제** 추가.
  삭제 후 전 스크립트 문법·import 해석·핵심 진입점 로드 스모크 테스트 통과.
- **`BIZ_KEYWORDS` 삭제** — 어디서도 안 쓰이는데 NUCLEAR 키가 없고 POWER 에 "원전"이 들어 있어,
  분류 기준을 오해하게 만들던 죽은 코드(현행 분류는 키워드 매칭이 아니라 AI 판정).
- 🔴 **"한 거 또 하는" 구조 제거 (토큰 낭비의 주범).**
  - 국회: `--only-new` 가 화면에 실린 *채택분*만 제외해서 **기각 7,764건이 증분마다 재판정**됐다.
    판정 아카이브 기준으로 거르도록 수정 → 증분 대상 **8,179건 → 0건**.
  - 판정을 되붙이는 기준을 배치 순번(id) → **내용 기반 안정 키**(`lib/tagkey.mjs`)로 교체.
    순번 방식은 수집이 한 건만 늘어도 엉뚱한 발언에 판정이 붙을 수 있는 구조였다(옛 판정은 자동 폴백).
    리팩터 전후 `quotes`/`stance` 완전 동일함을 대조 검증.
  - 청와대: **0건 판정된 회의가 기록에 안 남아** 매번 재판정됐다(46건 중 22건).
    `judgedMeetings` 대장 신설 + `cab_todo.mjs`(남은 것만 추림) → 재판정 대상 **46건 → 0건**.
- ⚡ **프롬프트 캐싱** — `wf_*.js` 프롬프트에서 변하는 부분(파일 경로·그룹 인덱스)을 맨 뒤로 옮겨
  공통 접두사를 **약 60자 → 1,133자**(wf_cabinet)로 확대. 각 파일에 순서를 깨지 말라는 주석 추가.
- 위 원칙을 워크스페이스 공통 지침(`C:\AI\CLAUDE.md`)에 사례와 함께 추가.

### 16:10 — ⚠️ 작업 중 낸 실수 기록 (재발 방지)
- 죽은 코드를 지운 뒤 "남은 스크립트가 잘 로드되나" 확인하려고 `node -e "import('./pipeline/scheduler.mjs')"`
  를 돌렸는데, **import 만으로 스케줄러가 실제 기동해 뉴스 수집(외부 API 수백 회)이 시작**됐다.
  즉시 종료했고 `data/news.json` 은 안 덮어써진 걸 확인(수집 완료 시점에만 쓰는 구조라 살았다).
- 원인: 모듈 최상위 코드는 import 즉시 실행된다. `scheduler.mjs` 는 최상위에서 타이머를 걸고 수집을 spawn 한다.
- 앞으로: **로드 검증은 `node --check`(문법)까지만.** 실행이 필요하면 그 파일이 최상위에서 부작용을 내는지
  (`if (process.argv[1] === ...)` 가드가 있는지) 먼저 열어볼 것. 워크스페이스 공통 함정 목록에도 추가했다.

### 16:00 — 오늘자 데이터 갱신 (증분)
- 청와대 회의록 **신규 0건**(2026-06-23이 최신, 다음 회차 행안부 미공개).
- 국회 발언 12,887 → **12,891건**(신규 4건 중 1건 채택). 화면 근거 발언 4,711 → **4,712건**.
  채택된 1건은 새 회의가 아니라 **`분산특구` 키워드 추가로 뒤늦게 매칭된 2024-10 발언**(서왕진)이라,
  분산에너지 우호 보드 1위가 바뀌었다.
- AI 종합분석은 **3명만** 재생성(정동만·박해철·김위상) — 어제 뉴스 라벨링을 AI 입력 생성 *뒤에* 돌려
  이 3명이 옛 라벨 기준이었다. 나머지 255명·보드 AI 요약은 입력이 동일해 그대로 뒀다.
- 뉴스는 손대지 않음 — NAS 스케줄러가 하루 5회 자동 수집 중이라 운영은 이미 최신.

## 2026-08-10

### 00:35 — 🔴 긴급 수정: `web/data.js`의 `window.APP_DATA` 대입문 유실로 화면 전체가 안 뜨던 문제
- 증상: 배포 사이트가 "데이터를 불러오지 못했습니다 (data.js)." 한 줄만 표시.
- 원인: 전날 `/rule-check` 지적("헤더 주석이 옛 스크립트명·6사업으로 낡음")을 고치면서
  `build_assembly_speeches.mjs`가 head를 새로 생성하도록 바꿨는데, 그때 **`window.APP_DATA = ` 대입문을
  빠뜨렸다.** 파일은 200 OK로 정상 크기(2.8MB) 내려가고 JSON 자체도 유효해서 네트워크·서버 쪽만 보면
  원인이 안 보인다 — `app.js:5`의 `if (!D)` 가드에만 걸린다.
- 조치: 생성 코드(`build_assembly_speeches.mjs`)에 대입문 복원 + 같은 실수 방지 주석, 이미 깨진
  `web/data.js`도 직접 복구. 나머지 생성 파일 3종(cabinet.js·news.js·bizboard.js)은 정상 확인.
- 교훈: 생성 스크립트의 head/tail을 건드릴 땐 **브라우저에서 실제로 렌더링되는지**까지 확인해야 한다.
  JSON 파싱 검증만으로는 이 사고가 안 잡힌다.

### 00:15 — 페이지 로딩 속도 개선(이미지 압축 + 캐시 헤더)
- 사용자가 배포된 사이트(7.yes-i-can.kr)가 느리다고 지적 → Network 타이밍 실측으로 원인 확인:
  로고(`logo.png` 707KB, 2200×712)와 대통령 명패(`president-plate.png` 1MB, 1921×819) 둘 다
  실제 화면 표시 크기(각 360px·250px 이하)보다 훨씬 큰 원본을 그대로 쓰고 있었음.
- `sharp`로 리사이즈(2배수 유지) — logo 707KB→26KB, president-plate 1MB→31KB. 페이지당 약 2.4MB 절감.
- `pipeline/serve.mjs`가 이미지까지 `Cache-Control: no-store`로 서빙해 새로고침마다 전체 재다운로드되던
  것도 확인 — 이미지만 `public, max-age=86400`(1일)으로 분리, JS/HTML은 재배포 반영 확인을 위해 no-store 유지.

## 2026-08-09

### 23:20 — SK E&S 사업 카테고리 6개 → 8개 확장 (에너지솔루션·분산에너지 추가)
- 사용자 요청: 기존 6개 사업(전력·LNG·재생E·수소·도시가스·원전)에 에너지솔루션(E/S)·분산에너지 2개 신규 카테고리 추가. 배터리·반도체(SK 계열사별 확장)·부동산은 이번 범위에서 제외(다음 주로 보류).
- 파이프라인 전체(`wf_cabinet.js`·`wf_ai.js`·`build_cabinet2.mjs`·`build_assembly_speeches.mjs`·`build_board_input2.mjs`·`build_board.mjs`·`build_ai_input.mjs`·`lib/news_labels.mjs`·`lib/config.mjs`)에 BIZ enum 8개로 확장, `web/data.js`의 `meta.businesses`에 반영.
- 국회 발언 12,887건 전량을 8개 카테고리로 재태깅(6카테고리 판정 폐기, 전면 재판정) — 4,711건 반영, 203명.
- 청와대 국무회의·차관회의는 다운로드 가능한 46개 회의(2026-01-23~06-23)만 8개 카테고리로 재판정, 그 이전(2025-07~2026-01-20, 98건)은 6카테고리 판정 그대로 보존(행안부 다운로드 400 에러로 재수집 불가).
- AI 종합분석 재생성(국회 224명·청와대 34명), 사업별 보드 재생성(8biz×3stance=24그룹, 111명 선정).
- 뉴스 라벨(`NEWS_LABELS`)에 분산에너지·에너지솔루션 키워드 추가, 기존 수집 기사 420건 재라벨링.
- 부수 발견: Workflow `args`가 JSON 문자열로 전달되는 플랫폼 버그를 발견해 전 workflow 스크립트에 방어 코드 추가.

### 18:30 — 데이터 출처 문서화
- 국무회의·차관회의 PDF(행정안전부 www.mois.go.kr)와 국회 상임위 발언(국회도서관 dataset.nanet.go.kr) 두 출처의 정확한 URL·인증 여부·수집 스크립트를 `pipeline/README.md` 상단 표로 정리 — 협업자가 자기 세션/크레딧으로 바로 수집할 수 있게.
- 행안부 다운로드가 2026-01-23 이전분(2025-07~2026-01-20)에서 전량 HTTP 400 실패하는 걸 `data/findings.json` dead_ends에 기록.

### 14:17 — 22대 국회 상임위 17개 전 위원회 발언 수집·태깅 완료
- 나머지 12개 위원회(국회운영·법제사법·정무·교육·외교통일·국방·행정안전·문화체육관광·농림축산식품해양수산·보건복지·정보·여성가족) 수집 — 4,908건 신규, 누적 12,887건.
- AI 태깅 698건 채택 → `web/data.js` 반영(591건, 97명) → AI 종합분석 재생성(248명) → 사업별 보드 재생성(85명).
- `DEPLOY.md` 낡은 문구 정정(컨테이너 2→3개, 비용 안내 범위 명시, 수집 시각 04:30→06~18시 5회).
- 🔄 업데이트 버튼 옆 "마지막 업데이트" 표시를 클릭 시각(localStorage) 대신 실제 뉴스 수집시각(`news.json meta.collectedAt`) 기준으로 변경 — 버튼을 안 눌러도 항상 정확.

### 12:55 — `/rule-check` 지적 4건 처리
- `UPDATE_GUIDE.md`에 노출돼 있던 실제 API 키·GOOGLE_CREDENTIALS를 플레이스홀더로 교체.
- 낡은 문서 2건 정정, 일회성 산출물 삭제.

### 00:58~12:15 — 국회 발언 DB 파이프라인 신규 확보 + 5개 위원회 수집
- `collect_assembly_speeches.mjs` 신설 — 국회도서관 발언 빅데이터(인증키 불필요)로 옛 파이프라인(끊겨 있던 `utt_ctx.json` 의존)을 대체.
- 기후에너지환경노동위·산업통상자원중소벤처기업위(전량) + 기획재정위·과학기술정보방송통신위·국토교통위(`--by-keyword`) 수집·태깅 — 발언 근거 472 → 3,704건, AI 분석 218명.
- `build_board_input2.mjs` 신설로 사업별 보드를 옛 스냅샷 문제 없이 재생성.

## 2026-08-08

### 저녁 — 청와대 회의록 파이프라인 정비
- 2단 조판·불릿 문자(U+110B) 버그로 유실됐던 발언 보강(230→296건), 정렬·중복 버그 수정.
- 조직도 각주에 회의록 공개 지연(회의 후 약 6주) 안내 추가.
- AI 종합분석 재생성(오탐 8건 제거).

### 오후 — 뉴스 자동수집 스케줄러 + NAS 자동배포 도입
- 기사 재수집을 하루 5회(06/09/12/15/18시)로 컨테이너 내 스케줄러화, 대량 실패 시 기존 데이터 보존하는 안전장치 추가.
- `labels` TDZ 버그로 전 의원 조회가 조용히 "실패" 처리되던 치명적 버그 수정 — 수집 0건이면 코드부터 의심할 것.
- NAS 자동배포(`autodeploy-loop.sh`, DSM 부팅트리거) 구성.
